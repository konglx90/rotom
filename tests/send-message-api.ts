/**
 * 通过 Master API 让"公司"给"小寿"发送消息
 *
 * 运行: node --import tsx tests/send-message-api.ts
 *
 * 注意: 这需要 Dashboard 的 WebSocket 连接支持
 * 或者需要实现一个 API 端点来代理发送消息
 */

// 注意: 目前 Master 的 REST API 不提供直接发送消息的功能
// 消息发送需要通过 Agent 的 WebSocket 连接

// ═══════════════════════════════════════════════════════════════
// 方案 A: 如果"公司"是 OpenClaw Agent，通过 LLM 工具调用
// ═══════════════════════════════════════════════════════════════

/*
在"公司"的 OpenClaw 中调用 LLM 工具:

1. 查看"小寿"是否在线:
   mesh_directory()

2. 发送消息给"小寿":
   mesh_send(
     target="小寿",
     message="你好小寿，我是公司"
   )
*/

console.log(`
========================================
  通过"公司"发送消息给"小寿"的方法
========================================

方法 1: 通过"公司"的 LLM 工具调用（推荐）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

在"公司"的对话中输入:

1. 查看通讯录:
   mesh_directory()

2. 发送消息给小寿:
   mesh_send(
     target="小寿",
     message="你好小寿，我是公司，收到请回复"
   )

方法 2: 通过 Dashboard（当前不支持）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Dashboard 目前只能查看历史消息，不支持发送。
需要添加此功能。

方法 3: 使用测试脚本（已验证可用）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

运行测试脚本验证功能:
  node --import tsx tests/simple-messaging-test.ts

方法 4: 通过 API 扩展（需要开发）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

可以添加一个 REST API 端点:
  POST /api/messages/send
  {
    "from": "公司",
    "to": "小寿",
    "message": "消息内容"
  }

这需要在 Master 中实现消息代理功能。

========================================
`);
